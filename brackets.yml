---
- hosts: localhost
  vars: 
    version: 1.0
    deb_file: /tmp/Brackets.{{version}}.Extract.64-bit.deb
  tasks:
    - name: download {{deb_file}}
      get_url: url=https://github.com/adobe/brackets/releases/download/release-{{version}}/Brackets.{{version}}.Extract.64-bit.deb dest={{deb_file}}
  
    - name: install Brackets
      raw: dpkg -i {{deb_file}}
      sudo: yes
